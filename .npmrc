# Fail fast on unsupported Node versions (engines: >=20) with a clear
# "Unsupported engine" error, instead of letting the install limp into
# cryptic build failures later (seen on a Node 18 server).
engine-strict=true
